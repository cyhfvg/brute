services:
  ssh-lab:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: alpine-ssh-lab

    ports:
      - "2222:22"

    restart: unless-stopped

    tty: true
    stdin_open: true
