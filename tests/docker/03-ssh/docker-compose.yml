services:
  ssh:
    build: .

    container_name: alpine-ssh-lab

    restart: "no"

    ports:
      - "2222:22"

    volumes:
      - ./init-users.sh:/init-users.sh

    command: >
      sh -c "
        chmod +x /init-users.sh &&
        /init-users.sh &&
        /usr/sbin/sshd -D -e
      "
